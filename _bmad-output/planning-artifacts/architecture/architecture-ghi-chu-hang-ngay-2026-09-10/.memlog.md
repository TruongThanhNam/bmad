---
scope: Toàn bộ sản phẩm 'Ghi chú hàng ngày' — web tĩnh một trang, local-first, không backend
purpose: build-substrate + deck + solution design + bản đồ chia việc
altitude: feature
updated: 2026-09-10T10:26
---

- (constraint) PRD NFR-5/NFR-4: không backend, không tài khoản, không telemetry; trang tĩnh trên GitHub Pages; mọi đọc/ghi trong máy sau khi tải xong
- (constraint) UX EXPERIENCE Foundation: vanilla HTML/CSS/JS, không UI library, single-surface, desktop-only Windows [ADOPTED bởi người dùng]
- (constraint) PRD NFR-6: chuẩn hóa tiếng Việt (bỏ dấu) là ràng buộc LƯU TRỮ/CHỈ MỤC, không xử lý ở lớp giao diện
- (constraint) PRD NFR-2: tìm + lọc trả kết quả ≤200ms với 2.000 ghi chú; NFR-1: mở tab tới gõ được ≤2s
- (constraint) PRD FR-19/FR-20 + Open Question 3: kiến trúc phải quyết ngưỡng cảnh báo dung lượng và cơ chế đa tab; FR-15 Open Question 4: quyết định dạng file sao lưu
- (constraint) PRD NFR-8: origin cố định vĩnh viễn — dữ liệu gắn với origin của GitHub Pages
- (decision) Lưu trữ tách hai kho theo vai trò: IndexedDB là nguồn sự thật duy nhất cho ghi chú; localStorage chỉ giữ trạng thái phiên nhỏ và cần đồng bộ (bản nháp, theme, mốc sao lưu gần nhất). Binds: FR-3, FR-19, NFR-1/2/3. Prevents: hai kho cùng giữ ghi chú rồi lệch nhau; bản nháp phải đợi DB mở; FR-19 phải đoán ngưỡng vì localStorage không có estimate()
- (decision) FR-20 giải bằng ĐỒNG BỘ THẬT qua BroadcastChannel, không phải cảnh báo khóa tab. Tab ghi phát tin, tab kia tải lại từ IndexedDB. Prevents: hai tab cùng ghi đè; và giữ được UJ-1 (để tab mở cả ngày)
- (decision) Không có bước build: ES modules nạp thẳng từ index.html, deploy = push lên GitHub Pages. npm/Vitest chỉ tồn tại để test phần logic thuần (bỏ dấu tiếng Việt, lọc, gộp file sao lưu). Prevents: toolchain hỏng sau vài tháng không đụng tới
- (question) DIVERGENCE cần báo ngược lên UX: EXPERIENCE.md Voice-and-Tone loại thông báo 2 ('Đang mở app ở một tab khác') không còn dùng nữa vì đã chọn đồng bộ thật. Phải đề nghị cập nhật EXPERIENCE.md ở bước Finalize
- (version) Xác minh trên web 2026-09-10: localStorage ~5MB/origin đếm theo UTF-16 (~2,5M ký tự), không có API hỏi quota; IndexedDB quota hàng trăm MB tới GB, đọc được qua navigator.storage.estimate(). Chuẩn hóa tiếng Việt: normalize('NFD') + bỏ \p{M} KHÔNG xử lý được đ/Đ — phải map tay trước khi normalize
- (decision) Paradigm: store một chiều (single state atom + action + render thuần) bọc trong ports-and-adapters. Core thuần không đụng DOM/IndexedDB. Prevents: nhiều đường đổi state; logic dễ sai (gộp, bỏ dấu, lọc) không test được nếu không tách khỏi trình duyệt
- (decision) Thời điểm tạo lưu là giờ tại chỗ + offset (ISO-8601 có offset, vd 2026-09-03T16:40:12+07:00). Hiển thị và lọc ngày LUÔN đọc phần giờ tại chỗ trong chuỗi, không bao giờ quy đổi sang múi giờ máy đang xem. Binds FR-2, FR-13, FR-16. Prevents: nạp lại trên máy khác múi giờ làm ghi chú trượt sang ngày khác, im lặng
- (decision) Toàn bộ ghi chú nạp vào một mảng trong RAM lúc mở app, kèm trường bỏ dấu tính sẵn. Tìm/lọc là phép quét mảng đồng bộ, không chạm IndexedDB. IndexedDB chỉ được đọc lúc khởi động và lúc BroadcastChannel báo có thay đổi. Binds NFR-2, FR-12, FR-13
- (decision) Origin cố định: https://truongthanhnam.github.io/bmad/ — app sống trong chính repo BMAD này, Pages phục vụ từ nhánh main thư mục gốc. HỆ QUẢ BẮT BUỘC: origin trình duyệt là https://truongthanhnam.github.io (không tính đường dẫn), dùng chung với mọi project site khác của tài khoản → tên IndexedDB và mọi key localStorage phải mang tiền tố riêng 'ghichu.'. Binds NFR-8
- (decision) Gọi navigator.storage.persist() ngay lần mở đầu. Dữ liệu IndexedDB mặc định là best-effort và bị trình duyệt dọn khi máy hết đĩa; NFR-3 nói ghi chú không tự mất đi. Nếu persist() bị từ chối, ghi nhận và dòng nhắc sao lưu FR-17 hạ ngưỡng xuống 3 ngày thay vì 7
- (decision) Ngưỡng FR-19: đọc navigator.storage.estimate() sau mỗi lần chốt/sửa/nạp lại; usage/quota ≥ 0.80 → dải băng cảnh báo trước; thao tác ghi ném lỗi QuotaExceededError → dải băng thất bại và state KHÔNG được cập nhật. Thay số đoán mò của UX bằng số đo thật
- (decision) Đáy trình duyệt: Chromium bản hiện hành (Edge/Chrome tự cập nhật) trên Windows. Không polyfill, không kiểm tra tính năng. Trả lời PRD Open Question 2 / A-10
- (decision) IndexedDB 'ghichu' v1: một object store 'notes' khóa chính là id; index 'localDate' trên chuỗi yyyy-MM-dd cắt từ phần giờ tại chỗ của createdAt. Mỗi ghi chú là MỘT bản ghi — không bao giờ ghi lại cả kho trong một thao tác. Binds FR-13, NFR-1
- (decision) Bản ghi ghi chú có đúng bốn trường: id (crypto.randomUUID), createdAt (ISO-8601 có offset), text, textFolded (bản bỏ dấu, chữ thường, tính sẵn). textFolded là dữ liệu dẫn xuất — luôn tính lại từ text khi ghi, không bao giờ được sửa riêng. Binds NFR-6, FR-12
- (decision) Bỏ dấu tiếng Việt là MỘT hàm thuần duy nhất dùng chung cho cả lúc ghi và lúc gõ tìm kiếm: map đ→d, Đ→D TRƯỚC, rồi normalize('NFD'), bỏ \p{M}, rồi toLowerCase. Prevents: hai chỗ bỏ dấu khác nhau → gõ đúng mà không ra kết quả
- (decision) File sao lưu là JSON: {schemaVersion:1, exportedAt, notes:[{id,createdAt,text}]}. textFolded KHÔNG có trong file (dẫn xuất, tính lại lúc nạp). Tên file ghi-chu-hang-ngay-YYYY-MM-DD.json. schemaVersion khác 1 → từ chối nạp, không đụng dữ liệu. Trả lời PRD Open Question 4 / A-7 / A-8
- (decision) Mọi lỗi hiện ở đúng một chỗ: dải băng dùng chung. Không console-only, không telemetry, không báo cáo ra ngoài (NFR-5). Một thao tác ghi thất bại thì state KHÔNG được cập nhật — giao diện không bao giờ tỏ ra đã lưu xong (FR-19)
- (decision) localStorage giữ đúng ba key có tiền tố: ghichu.draft, ghichu.theme, ghichu.lastBackupAt. Ghi bản nháp có debounce ≤500ms để giữ trần ≤1 giây của FR-3, và phát BroadcastChannel để tab kia thấy cùng bản nháp
- (event) kết thúc vòng coaching, bắt đầu chưng cất spine
- (event) vòng reviewer 1: lint sạch, 4 subagent (rubric, xác minh web, đối kháng, đối chiếu PRD+UX) trả về 3 finding chí mạng và nhiều finding nặng
- (decision) AD-3 mở rộng thành BA TẦNG phạm vi state (bền-dùng chung / bền-riêng tab / phù du). Bản nháp là tầng riêng-tab: key ghichu.draft.<tabId>, tabId trong sessionStorage, tab mới nhận nuôi bản nháp mồ côi lúc khởi động. Sửa lỗi chí mạng: AD-7 bản gốc phát tin bản nháp làm tab kia xóa mất chữ đang gõ — đúng thứ FR-20 cấm
- (decision) AD-8 tách hai luồng: đổi sự tồn tại (chốt/xóa/nạp) ghi-trước-state-sau; tự lưu nội dung (FR-3, FR-10) state-trước-ghi-sau với debounce 400ms, thất bại thì KHÔNG hoàn tác chữ, dải băng ở lại tới khi ghi sau thành công. Sửa mâu thuẫn chí mạng giữa AD-8 bản gốc và AD-1
- (decision) AD-7 định nghĩa hình dạng bản tin BroadcastChannel: {v, type: notes-changed|session-changed, from: tabId, appVersion}; không mang nội dung; bỏ qua tin của chính mình
- (decision) AD-4 siết: khóa sắp xếp là localStamp = createdAt.slice(0,19), khóa lọc là localDate = slice(0,10), cả hai từ core/time.js. Cấm so sánh chuỗi trên createdAt có offset (thứ tự sai với ghi chú nạp từ máy múi giờ khác) và cấm new Date() cho sắp/lọc/hiển thị
- (version) Sửa theo xác minh web: Vitest 3.x -> 5.0.0 (bản hiện hành 2026-09-03). Ngưỡng FR-19 đổi từ 'usage/quota>=0.80' sang '>=0.80 HOẶC còn <50MB trống' vì quota Chromium là phần lớn dung lượng ổ nên riêng vế 80% gần như không bao giờ nổ. AD-9 sửa lập luận sai: tiền tố chỉ ngăn va chạm tên, KHÔNG cách ly — ghi thành rủi ro chấp nhận có ý thức. AD-12 thêm ràng buộc secure context cho crypto.randomUUID. AD-10 đổi persist() sang thử lại mỗi lần khởi động
- (decision) Thêm AD-13..AD-21 bịt các lỗ reviewer tìm ra: hình dạng bản ghi 5 trường chỉ được nới ra; trần chặn ở core tại cả 3 cửa vào và query trả {items,total}; khối điều kiện duy nhất với 'hôm nay' là vắng mặt điều kiện; im lặng-khi-thành-công thành ràng buộc state; dải băng một chủ + thứ tự ưu tiên; tập mã lỗi đóng; tab title/theme là hàm của state; sàn a11y dựng được; APP_VERSION chống mã lệch phiên bản trong tab mở lâu
- (question) DIVERGENCE cần báo ngược lên UX (5 chỗ): EXPERIENCE.md loại thông báo 2 'Đang mở app ở một tab khác' đã chết ở 4 chỗ + DESIGN.md 1 chỗ; microcopy cảnh báo dung lượng 'còn khoảng 10%' không còn khớp ngưỡng mới. Và PRD Open Question 2 / A-10 đã được đóng (chốt Chromium) nên PRD nên cập nhật
- (event) spine sửa xong sau vòng reviewer 1, chạy vòng kiểm lại
- (decision) Bản nháp CHUYỂN từ localStorage sang IndexedDB store 'drafts' khóa tabId, có heartbeat. Lý do: chỉ IndexedDB có transaction nên việc nhận một bản nháp bỏ lại mới nguyên tử được; cơ chế nhận-nuôi trên localStorage ở vòng sửa trước có race (hai tab cùng nhận -> nhân đôi bản nháp -> ghi chú trùng id khác nhau mà AD-11 không dọn được) và làm đầy 5MB localStorage sau ~125 tab đã đóng. Chỉ nhận bản ghi đã hết nhịp tim -> tab đang gõ không bao giờ bị lấy mất bản nháp
- (decision) AD-8 thêm luật hủy hẹn: mỗi mục tiêu tự lưu mang seq tăng dần, hẹn nổ với seq cũ thì bị bỏ; chotGhiChu/xoaGhiChu hủy hẹn treo trước; chotGhiChu ghi note và làm rỗng draft trong MỘT transaction. Sửa lỗi chí mạng bản nháp ma -> ghi chú trùng
- (decision) AD-21 bỏ hẳn cơ chế ?v=<APP_VERSION> vì import tĩnh không đọc được hằng trong index.html — không bust được đồ thị import khi không có bước build. Thay bằng PHÁT HIỆN lệch phiên bản: APP_VERSION trong core/limits.js, đi trong mọi bản tin; tab lệch vào CHẾ ĐỘ CHỈ ĐỌC (không chỉ cảnh báo) vì mã cũ vẫn ghi được là đường hỏng thật
- (decision) Gộp cụm dải băng: AD-17 có một bảng ưu tiên 7 dòng là toàn bộ nguồn hợp lệ, AD-18 ánh xạ code sang số ưu tiên đó thay vì 'loại N' theo cách đánh số của UX. Thêm code VERSION_SKEW. Sửa mâu thuẫn AD-16/17/18/21
- (decision) Bốn chỗ vòng 1 bỏ sót nay đã bịt: fold() phải BẢO TOÀN CHỈ SỐ ký tự (test riêng) để tô từ khóa không lệch; AD-12 cấm mọi request mạng sau khi tải (webfont, CDN) để NFR-5 kiểm chứng được bằng tab Network; lastBackupAt cập nhật ở CẢ hai đường xuất và nạp; AD-20 nay dựng đủ 6 mục sàn a11y
- (event) vòng reviewer 2 xử lý xong, lint sạch
- (event) deck HTML tương tác xuất bản: https://claude.ai/code/artifact/dd429864-795f-4a98-85fc-c78bfcd40d05
- (event) bản đồ chia việc xong: phan-chia-cong-viec.md, 11 epic E0-E10, đường ngắn nhất tới dùng thật là E0-E1-E2-E3-E4-E7
- (event) cập nhật ngược upstream: EXPERIENCE.md (microcopy loại 2 đổi từ 'tab thứ hai' sang 'lệch phiên bản', bỏ số 10% ở 1b, sửa luật ưu tiên dải băng, chốt trình duyệt), DESIGN.md, mockups/key-canh-bao.html, và PRD (đóng Open Question 2/3/4, cập nhật A-7/A-8/A-10)
- (event) spine finalized
